# Remove the following lines that are causing warnings
# strict-peer-dependencies=false
# auto-install-peers=true
# node-linker=hoisted
# network-timeout=300000

# Instead, use these standard npm configurations if needed
save-exact=true
fund=false
